<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.gmathi.novellibrary.extension">

    <uses-feature android:name="novellibrary.extension" />

    <application android:icon="@mipmap/ic_launcher" android:allowBackup="false" android:label="${appName}">

        <meta-data android:name="novellibrary.extension.class" android:value="${extClass}" />
        <meta-data android:name="novellibrary.extension.factory" android:value="${extFactory}" />
        <meta-data android:name="novellibrary.extension.nsfw" android:value="${nsfw}" />

    </application>

</manifest>
